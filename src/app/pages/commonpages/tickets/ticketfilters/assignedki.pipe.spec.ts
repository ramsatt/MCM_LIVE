import { AssignedkiPipe } from './assignedki.pipe';

describe('AssignedkiPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignedkiPipe();
    expect(pipe).toBeTruthy();
  });
});
