import { BranchfilterPipe } from './branchfilter.pipe';

describe('BranchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BranchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
