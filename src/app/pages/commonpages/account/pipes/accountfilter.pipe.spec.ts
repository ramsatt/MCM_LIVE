import { AccountfilterPipe } from './accountfilter.pipe';

describe('AccountfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
