import { SelectedkiPipe } from './selectedki.pipe';

describe('SelectedkiPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedkiPipe();
    expect(pipe).toBeTruthy();
  });
});
