import { ShipmentprocessPipe } from './shipmentprocess.pipe';

describe('ShipmentprocessPipe', () => {
  it('create an instance', () => {
    const pipe = new ShipmentprocessPipe();
    expect(pipe).toBeTruthy();
  });
});
