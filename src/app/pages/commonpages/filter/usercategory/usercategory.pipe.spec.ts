import { UsercategoryPipe } from './usercategory.pipe';

describe('UsercategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new UsercategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
