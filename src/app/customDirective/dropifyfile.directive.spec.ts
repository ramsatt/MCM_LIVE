import { DropifyfileDirective } from './dropifyfile.directive';

describe('DropifyfileDirective', () => {
  it('should create an instance', () => {
    const directive = new DropifyfileDirective();
    expect(directive).toBeTruthy();
  });
});
