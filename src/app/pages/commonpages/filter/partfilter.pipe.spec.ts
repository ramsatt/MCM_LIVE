import { PartfilterPipe } from './partfilter.pipe';

describe('PartfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PartfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
