import { BranchcontractPipe } from './branchcontract.pipe';

describe('BranchcontractPipe', () => {
  it('create an instance', () => {
    const pipe = new BranchcontractPipe();
    expect(pipe).toBeTruthy();
  });
});
