import { SelectedvideoPipe } from './selectedvideo.pipe';

describe('SelectedvideoPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedvideoPipe();
    expect(pipe).toBeTruthy();
  });
});
