import { InstructionPipe } from './instruction.pipe';

describe('InstructionPipe', () => {
  it('create an instance', () => {
    const pipe = new InstructionPipe();
    expect(pipe).toBeTruthy();
  });
});
