import { TicketlistPipe } from './ticketlist.pipe';

describe('TicketlistPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketlistPipe();
    expect(pipe).toBeTruthy();
  });
});
