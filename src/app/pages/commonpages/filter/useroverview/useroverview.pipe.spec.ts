import { UseroverviewPipe } from './useroverview.pipe';

describe('UseroverviewPipe', () => {
  it('create an instance', () => {
    const pipe = new UseroverviewPipe();
    expect(pipe).toBeTruthy();
  });
});
