import { AccorderbyPipe } from './accorderby.pipe';

describe('AccorderbyPipe', () => {
  it('create an instance', () => {
    const pipe = new AccorderbyPipe();
    expect(pipe).toBeTruthy();
  });
});
