import { RaisedrequestPipe } from './raisedrequest.pipe';

describe('RaisedrequestPipe', () => {
  it('create an instance', () => {
    const pipe = new RaisedrequestPipe();
    expect(pipe).toBeTruthy();
  });
});
