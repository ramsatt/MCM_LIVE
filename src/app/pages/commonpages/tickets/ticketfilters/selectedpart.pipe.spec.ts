import { SelectedpartPipe } from './selectedpart.pipe';

describe('SelectedpartPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedpartPipe();
    expect(pipe).toBeTruthy();
  });
});
