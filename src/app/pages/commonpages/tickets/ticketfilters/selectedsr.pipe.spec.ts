import { SelectedsrPipe } from './selectedsr.pipe';

describe('SelectedsrPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedsrPipe();
    expect(pipe).toBeTruthy();
  });
});
