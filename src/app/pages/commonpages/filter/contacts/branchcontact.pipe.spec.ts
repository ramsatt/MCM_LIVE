import { BranchcontactPipe } from './branchcontact.pipe';

describe('BranchcontactPipe', () => {
  it('create an instance', () => {
    const pipe = new BranchcontactPipe();
    expect(pipe).toBeTruthy();
  });
});
