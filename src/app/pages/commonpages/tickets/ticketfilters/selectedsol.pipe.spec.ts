import { SelectedsolPipe } from './selectedsol.pipe';

describe('SelectedsolPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedsolPipe();
    expect(pipe).toBeTruthy();
  });
});
