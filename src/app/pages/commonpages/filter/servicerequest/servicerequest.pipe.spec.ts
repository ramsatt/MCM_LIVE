import { ServicerequestPipe } from './servicerequest.pipe';

describe('ServicerequestPipe', () => {
  it('create an instance', () => {
    const pipe = new ServicerequestPipe();
    expect(pipe).toBeTruthy();
  });
});
