import { DateconvertPipe } from './dateconvert.pipe';

describe('DateconvertPipe', () => {
  it('create an instance', () => {
    const pipe = new DateconvertPipe();
    expect(pipe).toBeTruthy();
  });
});
