import { UnassignedkiPipe } from './unassignedki.pipe';

describe('UnassignedkiPipe', () => {
  it('create an instance', () => {
    const pipe = new UnassignedkiPipe();
    expect(pipe).toBeTruthy();
  });
});
