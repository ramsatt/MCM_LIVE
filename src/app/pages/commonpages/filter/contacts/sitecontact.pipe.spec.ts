import { SitecontactPipe } from './sitecontact.pipe';

describe('SitecontactPipe', () => {
  it('create an instance', () => {
    const pipe = new SitecontactPipe();
    expect(pipe).toBeTruthy();
  });
});
