import { ShipmentcompanyPipe } from './shipmentcompany.pipe';

describe('ShipmentcompanyPipe', () => {
  it('create an instance', () => {
    const pipe = new ShipmentcompanyPipe();
    expect(pipe).toBeTruthy();
  });
});
