import { MailcontentPipe } from './mailcontent.pipe';

describe('MailcontentPipe', () => {
  it('create an instance', () => {
    const pipe = new MailcontentPipe();
    expect(pipe).toBeTruthy();
  });
});

