import { TechnicianPipe } from './technician.pipe';

describe('TechnicianPipe', () => {
  it('create an instance', () => {
    const pipe = new TechnicianPipe();
    expect(pipe).toBeTruthy();
  });
});
