import { SelectedinsPipe } from './selectedins.pipe';

describe('SelectedinsPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedinsPipe();
    expect(pipe).toBeTruthy();
  });
});
