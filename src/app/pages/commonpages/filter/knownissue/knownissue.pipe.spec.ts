import { KnownissuePipe } from './knownissue.pipe';

describe('KnownissuePipe', () => {
  it('create an instance', () => {
    const pipe = new KnownissuePipe();
    expect(pipe).toBeTruthy();
  });
});
