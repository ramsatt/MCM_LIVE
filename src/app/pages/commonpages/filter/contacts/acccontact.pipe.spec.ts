import { AcccontactPipe } from './acccontact.pipe';

describe('AcccontactPipe', () => {
  it('create an instance', () => {
    const pipe = new AcccontactPipe();
    expect(pipe).toBeTruthy();
  });
});
